# OCSD Core Dependencies
# Install with: pip install -r requirements.txt
#
# For optional features, use pyproject.toml extras instead:
#   pip install -e ".[embeddings]"   # CLIP + FAISS
#   pip install -e ".[yoloe]"        # YOLOE visual grounding
#   pip install -e ".[vlm]"          # VLM via LiteLLM
#   pip install -e ".[api]"          # FastAPI REST server
#   pip install -e ".[windows]"      # Windows accessibility bridge
#   pip install -e ".[all]"          # Everything
#   pip install -e ".[dev]"          # Testing tools (pytest, ruff)

mss>=9.0
Pillow>=10.0
numpy>=1.24
opencv-python>=4.8
pytesseract>=0.3.10
python-dotenv>=1.0.0
pyautogui>=0.9.54
pynput>=1.7
networkx>=3.0
pyyaml>=6.0
PyQt6>=6.5
